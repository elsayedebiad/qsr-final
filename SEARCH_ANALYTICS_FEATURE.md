# ميزة تحليلات البحث والفلاتر

## 📊 نظرة عامة

تم إضافة نظام متكامل لتتبع وتحليل جميع عمليات البحث والفلاتر التي يقوم بها الزوار في صفحات المبيعات. يساعد هذا النظام المدير العام على فهم احتياجات العملاء وتحسين المحتوى والتوزيع.

## ✨ الميزات الرئيسية

### 1. تسجيل تلقائي لعمليات البحث
- تسجيل كلمات البحث التي يكتبها الزوار
- تتبع جميع الفلاتر المستخدمة (الجنسية، الوظيفة، العمر، المهارات، إلخ)
- حفظ عدد النتائج لكل بحث
- تسجيل معلومات الجلسة وعنوان IP

### 2. صفحة تحليلات شاملة
- **الموقع**: Dashboard → تحليلات البحث والفلاتر
- **الوصول**: للأدمن والمطور فقط
- **المسار**: `/dashboard/search-analytics`

### 3. إحصائيات تفصيلية
- إجمالي عمليات البحث
- أكثر كلمات البحث استخداماً
- أكثر الجنسيات المطلوبة
- أكثر الوظائف المبحوث عنها
- المهارات الأكثر طلباً
- توزيع عمليات البحث حسب صفحات المبيعات

### 4. فلترة متقدمة
- اختيار صفحة مبيعات محددة أو جميع الصفحات
- تحديد نطاق زمني مخصص
- تصدير البيانات إلى CSV
- تحديث فوري للبيانات

## 🗂️ الملفات المضافة/المعدلة

### ملفات جديدة:
1. **صفحة التحليلات**
   - `src/app/dashboard/search-analytics/page.tsx`
   - واجهة مستخدم كاملة مع نفس ثيم النظام

2. **دالة تسجيل البحث**
   - `src/lib/search-analytics.ts`
   - دوال مساعدة لتسجيل عمليات البحث

### ملفات معدلة:
1. **القائمة الجانبية**
   - `src/components/app-sidebar.tsx`
   - إضافة رابط الصفحة الجديدة في القائمة

2. **صفحات المبيعات** (مطلوب للتفعيل الكامل)
   - `src/app/sales1/page.tsx`
   - استيراد دالة `logSearchAnalytics`
   - إضافة useEffect لتتبع التغييرات

## 📦 قاعدة البيانات

النظام يستخدم جدول `SearchAnalytics` الموجود في schema.prisma:

```prisma
model SearchAnalytics {
  id            Int      @id @default(autoincrement())
  salesPageId   String   // sales1, sales2, ... sales11
  searchTerm    String?  // كلمة البحث
  nationality   String?  // فلتر الجنسية
  position      String?  // فلتر الوظيفة
  ageFilter     String?  // فلتر العمر
  experience    String?  // فلتر الخبرة
  arabicLevel   String?  // فلتر اللغة العربية
  englishLevel  String?  // فلتر اللغة الإنجليزية
  maritalStatus String?  // فلتر الحالة الاجتماعية
  skills        String[] @default([]) // المهارات المحددة
  religion      String?  // فلتر الديانة
  education     String?  // فلتر التعليم
  ipAddress     String?  // عنوان IP
  userAgent     String?  // معلومات المتصفح
  sessionId     String?  // معرف الجلسة
  resultsCount  Int?     // عدد النتائج
  timestamp     DateTime @default(now())

  @@index([salesPageId, timestamp])
  @@index([searchTerm])
  @@index([nationality])
  @@index([position])
  @@map("search_analytics")
}
```

## 🚀 كيفية الاستخدام

### للمدير:
1. افتح Dashboard
2. اذهب إلى "تحليلات البحث والفلاتر" من القائمة الجانبية
3. اختر صفحة المبيعات المطلوبة أو "جميع الصفحات"
4. حدد النطاق الزمني المطلوب
5. اضغط "تحديث" لتحديث البيانات
6. يمكنك تصدير البيانات بالضغط على "تصدير"

### للمطور:
لتفعيل التتبع في صفحات مبيعات إضافية:

1. استورد الدالة في أعلى الملف:
```typescript
import { logSearchAnalytics } from '@/lib/search-analytics'
```

2. أضف useEffect لتتبع التغييرات:
```typescript
useEffect(() => {
  if (cvs.length === 0) return

  logSearchAnalytics({
    salesPageId: 'sales1', // غير هذا حسب الصفحة
    searchTerm: searchTerm || undefined,
    nationality: nationalityFilter !== 'ALL' ? nationalityFilter : undefined,
    position: positionFilter !== 'ALL' ? positionFilter : undefined,
    // ... باقي الفلاتر
    resultsCount: allFilteredCvs.length
  })
}, [/* dependencies */])
```

## 📊 التقارير المتاحة

### 1. كلمات البحث الأكثر شيوعاً
- عرض أكثر 20 كلمة بحث
- عدد مرات البحث لكل كلمة
- مفيد لفهم ما يبحث عنه العملاء

### 2. الجنسيات المطلوبة
- أكثر الجنسيات التي يبحث عنها العملاء
- يساعد في تحديد أولويات الاستيراد

### 3. الوظائف المطلوبة
- أكثر الوظائف طلباً
- يساعد في تخصيص المحتوى

### 4. المهارات المطلوبة
- المهارات الأكثر طلباً من العملاء
- مفيد لتدريب الموظفين

### 5. السجل التفصيلي
- عرض آخر 100 عملية بحث
- التاريخ والوقت
- جميع الفلاتر المستخدمة
- عدد النتائج

## 🎨 التصميم

الصفحة تتبع نفس ثيم النظام:
- ✅ دعم الوضع الداكن (Dark Mode)
- ✅ تصميم متجاوب (Responsive)
- ✅ ألوان موحدة مع النظام
- ✅ أيقونات Lucide React
- ✅ مكونات Shadcn UI

## 🔒 الأمان والخصوصية

- ❌ لا يتم تسجيل أي معلومات شخصية للمستخدمين
- ✅ تسجيل عنوان IP فقط للإحصائيات
- ✅ Session ID عشوائي لتتبع الجلسة فقط
- ✅ الوصول محصور للأدمن والمطور

## 📈 الأداء

- **Debouncing**: تأخير 1 ثانية قبل التسجيل لتقليل الطلبات
- **Deduplication**: تجنب تسجيل نفس الفلاتر مرتين متتاليتين
- **Session Tracking**: معرف جلسة واحد لكل زائر
- **Indexing**: فهرسة الحقول الأكثر استخداماً

## 🔄 التحديثات المستقبلية المقترحة

1. إضافة رسوم بيانية تفاعلية
2. تقارير أسبوعية/شهرية تلقائية
3. مقارنة بين صفحات المبيعات
4. تنبيهات عند ملاحظة اتجاهات جديدة
5. تصدير تقارير PDF
6. تكامل مع Google Analytics

## ✅ قائمة التحقق

- [x] إنشاء جدول قاعدة البيانات
- [x] إنشاء API endpoint للتسجيل
- [x] إنشاء API endpoint للاستعلام
- [x] إنشاء صفحة التحليلات
- [x] إضافة الصفحة للقائمة الجانبية
- [x] إنشاء دالة التسجيل المساعدة
- [x] تطبيق على صفحة sales1 (مثال)
- [ ] تطبيق على باقي صفحات المبيعات (sales2-11)

## 📝 ملاحظات

- النظام جاهز للاستخدام الفوري
- يمكن البدء في جمع البيانات من اليوم
- لتطبيق على كل صفحات المبيعات، انسخ الكود من sales1
- البيانات تُحفظ بشكل دائم في قاعدة البيانات

## 🆘 الدعم

في حالة وجود أي مشاكل أو استفسارات:
1. تحقق من console للأخطاء
2. تأكد من أن جدول SearchAnalytics موجود في قاعدة البيانات
3. تحقق من صلاحيات المستخدم

---

**تم الإنشاء في**: 21 نوفمبر 2025  
**الحالة**: ✅ جاهز للإنتاج
