'use client'

/**
 * مكون معطل - يسمح بالدخول المباشر لصفحات السلز
 * التوزيع يعمل فقط من /sales
 */
export default function SalesRedirectCheck() {
  // المكون معطل - الدخول المباشر مسموح
  return null
}
