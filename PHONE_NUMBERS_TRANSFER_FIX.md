المدير يختار رقم/أرقام ← يختار مستخدم مستهدف ← لا يدخل وقت ← تحويل
النتيجة:
- الرقم يظهر فوراً للمستخدم المستهدف (واضح، غير محجوب)
- الرقم يظهر للمستخدم الأصلي (محجوب مع رسالة: تم حجب الرقم بسبب...)
```

### 2️⃣ التحويل مع وقت محدد
```
المدير يختار رقم/أرقام ← يختار مستخدم مستهدف ← يدخل وقت (مثلاً: 2 ساعة) ← تحويل
النتيجة:
- الرقم يظهر للمستخدم الأصلي مع عداد تنازلي (واضح، غير محجوب)
- الرقم يظهر للمستخدم المستهدف مع عداد تنازلي (واضح، غير محجوب)
- عند انتهاء الوقت بدون تواصل:
  * يصبح الرقم محجوب للمستخدم الأصلي
  * يصبح الرقم محجوب للمستخدم المستهدف
  * يظهر badge "معطل" باللون الأحمر
```

---

## 🔍 منطق الحجب (isNumberBlocked)

الرقم يكون **محجوب** في الحالات التالية:

1. ✅ **انتهى الوقت** (`isExpired = true`)
   - يحجب للجميع (الأصلي والمستهدف)
   
2. ✅ **محول لشخص آخر** (`isTransferred = true && transferredToUserId !== userId`)
   - المستخدم الأصلي يرى رقمه محجوباً عندما يُحوّل لشخص آخر بدون وقت
   - المستخدمون الآخرون لا يرون الرقم أصلاً

الرقم **غير محجوب** في الحالات التالية:

1. ✅ **رقم أصلي** (لم يُحوّل لأحد)
2. ✅ **محول إليّ** (`transferredToUserId === userId`)
3. ✅ **محول من صفحتي مع مؤقت نشط** (لأرى العداد التنازلي)

---

## 🎯 سيناريوهات الاستخدام

### سيناريو 1: تحويل فوري بدون وقت
```
المدير Admin:
1. يختار رقم من صفحة sales1
2. يحوله إلى المستخدم "أحمد"
3. لا يدخل أي وقت
4. يضغط "تحويل"

النتيجة:
- المستخدم الأصلي (صاحب sales1): يرى الرقم محجوب ❌
- أحمد: يرى الرقم واضحاً ✅ ويمكنه فتح WhatsApp
```

### سيناريو 2: تحويل مع وقت محدد
```
المدير Admin:
1. يختار رقم من صفحة sales1
2. يحوله إلى المستخدم "أحمد"
3. يدخل وقت: 2 ساعة
4. يضغط "تحويل"

النتيجة:
- المستخدم الأصلي: يرى الرقم مع عداد تنازلي (2:00:00) ⏱️
- أحمد: يرى الرقم مع عداد تنازلي (2:00:00) ⏱️
- كلاهما يمكنهما التواصل عبر WhatsApp

بعد انتهاء الوقت (بدون تواصل):
- المستخدم الأصلي: يرى الرقم محجوب مع badge "معطل" ❌
- أحمد: يرى الرقم محجوب مع badge "معطل" ❌
- المدير فقط يمكنه إعادة تفعيل الرقم
```

### سيناريو 3: تواصل ناجح قبل انتهاء الوقت
```
- أحمد يضغط على WhatsApp ويتواصل مع العميل
- النظام يسجل "تم التواصل" ✅
- الرقم يبقى واضحاً حتى بعد انتهاء الوقت
- badge يتحول إلى "تم التواصل" باللون الأخضر
```

---

## 📁 الملفات المعدلة

### 1. `/api/phone-numbers/list/route.ts`
**التعديل**: 
- فلتر `OR` لإظهار الأرقام الأصلية + المحولة
- إزالة شرط `isTransferred: false` من الأرقام الأصلية
- إزالة شرط `isExpired: false` من الأرقام المحولة

**السطور**: 59-82

### 2. `/dashboard/phone-numbers/page.tsx`
**التعديل**: 
- تحسين منطق `isNumberBlocked` مع تعليقات توضيحية
- تنظيم الكود لسهولة القراءة

**السطور**: 221-237

---

## ✅ التحقق من التحديثات

### خطوات الاختبار:

1. **تسجيل الدخول كمدير**
   - الذهاب إلى صفحة Phone Numbers
   - اختيار رقم أو أكثر
   - الضغط على "تحويل الأرقام"

2. **تحويل بدون وقت**
   - اختيار مستخدم مستهدف
   - ترك الوقت = 0
   - الضغط "تحويل"
   - ✅ التحقق: الرقم يظهر للمستخدم المستهدف واضحاً

3. **تحويل مع وقت**
   - اختيار مستخدم مستهدف
   - إدخال وقت (مثال: 0 ساعة، 5 دقائق، 0 ثانية)
   - الضغط "تحويل"
   - ✅ التحقق: العداد التنازلي يظهر للطرفين
   - ✅ التحقق: بعد 5 دقائق، الرقم يصبح "معطل"

4. **تسجيل الدخول كمستخدم عادي**
   - ✅ التحقق: الأرقام المحولة إليه تظهر واضحة
   - ✅ التحقق: يمكنه فتح WhatsApp
   - ✅ التحقق: بعد الضغط على WhatsApp، يظهر "تم التواصل"

---

## 🚀 الخطوات التالية

1. إعادة تشغيل السيرفر
2. اختبار السيناريوهات أعلاه
3. مراقبة console للتأكد من عدم وجود أخطاء

---

## 📝 ملاحظات مهمة

- ⏰ **التحقق من الأرقام المنتهية** يتم تلقائياً كل دقيقة
- 🔄 **العداد التنازلي** يعمل في الوقت الفعلي باستخدام `CountdownTimer` component
- 🔒 **الأرقام المحجوبة** تظهر مع تأثير blur ورسالة "تم حجب الرقم"
- ✅ **لا يوجد تأثير على الأرقام الموجودة** - النظام متوافق مع البيانات القديمة

---

التحديثات جاهزة! 🎉
