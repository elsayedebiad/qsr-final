# 📊 دليل التحكم في المسافات بين أعمدة الجداول

## 🎯 التغييرات التي تمت

تم تقليل المسافات بين الأعمدة في جميع جداول لوحة التحكم لجعلها أكثر تناسقاً وقابلة للتخصيص.

### التغييرات الرئيسية:

#### 1️⃣ **متغيرات CSS الجديدة** (في `src/app/globals.css`)
تم إضافة متغيرات CSS قابلة للتخصيص:

```css
/* Table Spacing Variables - يمكن تخصيصها بسهولة */
--table-cell-padding-x: 0.75rem;      /* 12px - المسافة الأفقية بين محتوى الخلايا */
--table-cell-padding-y: 0.5rem;       /* 8px - المسافة الرأسية بين محتوى الخلايا */
--table-header-padding-x: 0.75rem;    /* 12px - المسافة الأفقية في رأس الجدول */
--table-header-padding-y: 0.625rem;   /* 10px - المسافة الرأسية في رأس الجدول */
```

#### 2️⃣ **الجداول المحدّثة:**
- ✅ **جدول السير الذاتية الرئيسي** (`/dashboard`) - تم التقليل من `px-4 py-4` و `px-3 py-4` إلى `px-3 py-2.5` و `px-2 py-2.5`
- ✅ **جدول الأرشيف** (`/dashboard/archive`) - تم التقليل من `px-4 py-4` و `px-3 py-4` إلى `px-3 py-2.5` و `px-2 py-2.5`
- ✅ **جدول المستخدمين** (`/dashboard/users`) - تم التقليل من `px-6 py-4` إلى `px-3 py-3`
- ✅ **جدول الزيارات** (`/dashboard/visits`) - تم التقليل من `px-6 py-4` إلى `px-3 py-3`
- ✅ **جدول تقرير الزيارات** (`/dashboard/visits-report`) - تم التقليل من `py-3` إلى `py-2`
- ✅ **جدول التعاقدات** (`/dashboard/contracts`) - يستخدم class `.table` الذي تم تحديثه

---

## 🛠️ كيفية التخصيص

### **الطريقة 1: تعديل المتغيرات العامة (مُوصى به)**

لتغيير المسافات في جميع الجداول، عدّل المتغيرات في `src/app/globals.css`:

```css
:root {
  /* قيم أصغر = مسافات أقل */
  --table-cell-padding-x: 0.5rem;   /* مثال: 8px بدلاً من 12px */
  --table-cell-padding-y: 0.375rem; /* مثال: 6px بدلاً من 8px */
  
  /* قيم أكبر = مسافات أكثر */
  --table-cell-padding-x: 1rem;     /* مثال: 16px بدلاً من 12px */
  --table-cell-padding-y: 0.75rem;  /* مثال: 12px بدلاً من 8px */
}
```

### **الطريقة 2: تخصيص جدول معين**

لتخصيص جدول واحد فقط، استخدم Tailwind classes مباشرة في ملف الصفحة:

```tsx
// مثال: جدول أكثر ضغطاً
<th className="px-2 py-1.5">العمود</th>
<td className="px-2 py-2">البيانات</td>

// مثال: جدول أكثر اتساعاً
<th className="px-4 py-3">العمود</th>
<td className="px-4 py-3.5">البيانات</td>
```

---

## 📐 مرجع سريع للمسافات (Tailwind)

| Class | القيمة (rem) | القيمة (px) | الاستخدام المقترح |
|-------|-------------|------------|-------------------|
| `px-1` `py-1` | 0.25rem | 4px | جداول مضغوطة جداً |
| `px-2` `py-2` | 0.5rem | 8px | جداول مضغوطة (حالياً في visits-report) |
| `px-3` `py-2.5` | 0.75rem / 0.625rem | 12px / 10px | **القيمة الحالية** (متوازنة) |
| `px-4` `py-3` | 1rem / 0.75rem | 16px / 12px | جداول مريحة |
| `px-6` `py-4` | 1.5rem / 1rem | 24px / 16px | جداول واسعة (القيمة القديمة) |

---

## 💡 نصائح

1. **للجداول الكبيرة** (الكثير من الأعمدة): استخدم مسافات أصغر `px-2` للحفاظ على العرض
2. **للجداول الصغيرة** (أعمدة قليلة): يمكن استخدام مسافات أكبر `px-4` لمظهر أفضل
3. **المسافات الرأسية** (`py-`): أقل من الأفقية عادةً لجعل الصفوف أكثر تراصاً
4. **التناسق**: حاول الحفاظ على نفس المسافات في جميع جداول الصفحة الواحدة

---

## 🔄 للرجوع للإعدادات القديمة

إذا أردت الرجوع للمسافات الأكبر، عدّل في `globals.css`:

```css
:root {
  --table-cell-padding-x: 1.5rem;   /* 24px (القيمة القديمة) */
  --table-cell-padding-y: 1rem;     /* 16px (القيمة القديمة) */
  --table-header-padding-x: 1.5rem;
  --table-header-padding-y: 0.75rem;
}
```

---

## 📝 ملاحظات

- ⚠️ تحذير lint في `globals.css` عن `@theme` هو warning عادي من Tailwind v4 ولا يؤثر على الوظائف
- ✨ جميع التغييرات responsive ومتوافقة مع Dark Mode
- 🎨 المتغيرات تعمل في Dark Mode و Light Mode تلقائياً

---

## 🆘 المساعدة

للاستفسارات أو التخصيصات الإضافية، يمكنك:
1. تعديل ملف `src/app/globals.css` (للتغييرات العامة)
2. تعديل الملفات الفردية في `src/app/dashboard/*/page.tsx` (للتخصيصات الخاصة)

---

تم التحديث: نوفمبر 2024
