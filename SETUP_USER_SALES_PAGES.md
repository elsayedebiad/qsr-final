# إعداد ميزة تخصيص صفحات المبيعات

## ⚠️ خطوات مهمة قبل الاستخدام

### 1. إيقاف الخادم
أولاً، يجب إيقاف خادم التطوير (إذا كان يعمل):
- اضغط `Ctrl+C` في Terminal الذي يعمل فيه الخادم

### 2. إعادة توليد Prisma Client
```bash
npx prisma generate
```

### 3. تشغيل Migration
```bash
npx prisma migrate dev --name add_user_sales_pages
```

أو إذا كنت تريد فقط دفع التغييرات بدون migration:
```bash
npx prisma db push
```

### 4. إعادة تشغيل الخادم
```bash
npm run dev
```

## 🔍 التحقق من التثبيت

بعد إعادة تشغيل الخادم، تحقق من:
1. افتح `/dashboard/user-sales-pages`
2. يجب أن تظهر الصفحة بدون أخطاء
3. يمكنك إضافة تخصيصات جديدة

## ❌ حل المشاكل

### خطأ: "Cannot read properties of undefined (reading 'findUnique')"
**السبب**: Prisma Client لم يتم إعادة توليده بعد إضافة النموذج الجديد

**الحل**:
1. أوقف الخادم
2. شغل: `npx prisma generate`
3. شغل: `npx prisma migrate dev --name add_user_sales_pages`
4. أعد تشغيل الخادم

### خطأ: "Table does not exist"
**السبب**: Migration لم يتم تشغيله

**الحل**:
```bash
npx prisma migrate dev --name add_user_sales_pages
```

### خطأ: "EPERM: operation not permitted"
**السبب**: الملف قيد الاستخدام من قبل الخادم

**الحل**:
1. أوقف الخادم تماماً
2. شغل الأوامر مرة أخرى
3. أعد تشغيل الخادم

## 📝 ملاحظات

- تأكد من أن قاعدة البيانات متصلة
- تأكد من أن ملف `.env` يحتوي على `DATABASE_URL` الصحيح
- بعد أي تغيير في `schema.prisma`، يجب إعادة توليد Prisma Client

