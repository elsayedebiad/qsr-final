# ✅ إضافة Card "المحولة" في الإحصائيات

## الميزة الجديدة:

### Card جديدة في Stats Cards - "المحولة" 🔴

**للأدمن فقط** - تعرض عدد الأرقام المحولة (المنتهية)

---

## 🎨 التصميم:

```
┌─────────────────────┐
│ 🔴 المحولة          │
│ 15                  │
└─────────────────────┘
```

### الألوان:
- **Border**: أحمر (red-500)
- **Background**: تدرج أحمر فاتح
- **Text**: أحمر غامق
- **Icon**: AlertCircle

### التفاعل:
- **Clickable**: ✅ الضغط عليها يفتح/يغلق قائمة المحولة
- **Hover**: تأثير رفع (hover-lift)

---

## 📊 الموقع:

```
[إجمالي الأرقام] [المحولة] [sales1] [sales2] [sales3] ...
      ↓              ↓
   أزرق          أحمر
```

---

## 🔧 كيف يعمل:

### 1. **API** (`/api/phone-numbers/list/route.ts`):
```typescript
// حساب عدد الأرقام المحولة (للأدمن فقط)
let transferredCount = 0
if (user.role === 'ADMIN') {
  transferredCount = await db.phoneNumber.count({
    where: {
      isArchived: false,      // غير مؤرشفة
      isExpired: true,        // منتهية
      isTransferred: true     // محولة
    }
  })
}

return {
  stats: statsBySalesPage,
  transferredCount: transferredCount  // ← العدد الجديد
}
```

### 2. **Frontend** (`page.tsx`):
```tsx
// State جديد
const [transferredCount, setTransferredCount] = useState(0)

// حفظ القيمة من API
setTransferredCount(data.transferredCount || 0)

// Card قابلة للضغط
<div 
  className="card border-red-500 bg-red-gradient"
  onClick={() => setShowExpired(!showExpired)}
>
  <AlertCircle /> المحولة
  <div>{transferredCount}</div>
</div>
```

---

## 🎯 السيناريوهات:

### **سيناريو 1: الأدمن يفتح الصفحة**
```
Stats Cards:
┌─────────┐ ┌─────────┐ ┌─────────┐
│ إجمالي  │ │ المحولة │ │ sales1  │
│   150   │ │   15    │ │   45    │
└─────────┘ └─────────┘ └─────────┘
```

### **سيناريو 2: الأدمن يضغط على "المحولة"**
```
✅ Card المحولة تصبح active (لون أحمر قوي)
↓
الجدول يعرض الأرقام المحولة فقط (15 رقم)
```

### **سيناريو 3: مستخدم عادي**
```
Stats Cards:
┌─────────┐ ┌─────────┐
│ إجمالي  │ │ sales1  │  ← المحولة لا تظهر!
│   45    │ │   45    │
└─────────┘ └─────────┘
```

---

## ✨ الفوائد:

### ✅ **نظرة سريعة**:
- الأدمن يرى عدد الأرقام المحولة بنظرة واحدة
- بدون الحاجة للبحث أو الفلترة

### ✅ **تفاعلية**:
- الضغط على Card = فتح قائمة المحولة
- سهولة الوصول

### ✅ **تصميم واضح**:
- لون أحمر يميزها
- أيقونة AlertCircle مفهومة

### ✅ **للأدمن فقط**:
- لا تُربك المستخدمين العاديين
- معلومات إدارية فقط

---

## 📁 التعديلات:

### 1. **API** (`list/route.ts`):
- إضافة حساب `transferredCount` (السطور: 128-138)
- إضافة `transferredCount` في الـ response (السطر: 148)

### 2. **Frontend** (`page.tsx`):
- إضافة state `transferredCount` (السطر: 75)
- حفظ القيمة من API (السطر: 314)
- إضافة Card "المحولة" (السطور: 497-510)

---

## 🎨 التصميم النهائي:

```
┌──────────────────── Stats Cards ────────────────────┐
│                                                      │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐             │
│  │إجمالي   │  │🔴 محولة│  │ sales1  │  ...        │
│  │  150    │  │   15    │  │   45    │             │
│  └─────────┘  └─────────┘  └─────────┘             │
│                   ↓                                  │
│              يمكن الضغط عليها                       │
└──────────────────────────────────────────────────────┘
```

---

**جاهز! الأدمن الآن لديه card مخصصة للأرقام المحولة! 🎉**
