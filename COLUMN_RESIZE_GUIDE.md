# 📏 دليل تغيير حجم الأعمدة (Column Resizing)

## 🎯 ميزة جديدة: تغيير عرض الأعمدة مثل Excel

تم إضافة إمكانية تغيير عرض أعمدة الجدول بشكل ديناميكي مثل Microsoft Excel!

### ✨ الميزات:

1. **خطوط فاصلة مرئية** بين كل عمودين
2. **سحب وإفلات** لتغيير العرض بسهولة
3. **تأثيرات بصرية** عند التمرير والسحب
4. **حد أدنى للعرض** (50px) لمنع الأعمدة من أن تصبح صغيرة جداً
5. **💾 حفظ تلقائي** - الإعدادات تُحفظ تلقائياً ولا تحتاج لإعادة التعديل
6. **🔄 إعادة ضبط** - زر لإعادة الأعمدة للعرض الافتراضي
7. **🔤 التحكم في حجم الخط** - تكبير وتصغير الخط (10-20px) مع حفظ تلقائي

---

## 🖱️ كيفية الاستخدام:

### الطريقة:
1. **مرر الماوس** على الخط الفاصل بين عمودين في رأس الجدول
2. سيتحول الخط إلى **اللون الأزرق** ويصبح أعرض قليلاً
3. **اضغط واسحب** الخط يميناً أو يساراً
4. **أفلت الماوس** عندما تصل للعرض المطلوب

### التأثيرات المرئية:
- 🔵 **لون أزرق فاتح** عند التمرير على الخط
- 🔷 **لون أزرق داكن** عند السحب
- ↔️ **مؤشر السحب** (col-resize cursor) يظهر تلقائياً
- ✨ **انتقال سلس** للعرض

### 💾 الحفظ التلقائي:
- ✅ **يُحفظ تلقائياً** في localStorage بعد كل تعديل
- ✅ **يعمل لكل حساب** - كل مستخدم له إعداداته الخاصة
- ✅ **يبقى محفوظاً** حتى بعد إغلاق المتصفح
- 🔄 **زر إعادة الضبط** في أعلى الجدول لإرجاع القيم الافتراضية

### 🔤 التحكم في حجم الخط:
- **A-** لتصغير الخط
- **A+** لتكبير الخط
- **عرض الحجم الحالي** (مثل: 14px)
- **نطاق الأحجام**: 10px إلى 20px
- **↻** لإعادة الخط للحجم الافتراضي (14px)
- 💾 **يُحفظ تلقائياً** في localStorage

📖 **للمزيد**: راجع ملف `FONT_SIZE_CONTROL.md`

---

## 🎨 التفاصيل التقنية:

### 🗄️ تخزين البيانات:

**عرض الأعمدة:**
- **المفتاح**: `cvTableColumnWidths`
- **الصيغة**: JSON object مع عرض كل عمود
- **التحديث**: تلقائياً بعد كل تغيير

**حجم الخط:**
- **المفتاح**: `cvTableFontSize`
- **الصيغة**: رقم (string) يمثل البكسل (مثل: "14")
- **التحديث**: تلقائياً بعد كل تغيير

**القراءة**: عند تحميل الصفحة

### الأعمدة المتاحة للتعديل:
- ✅ عمود التحديد (Checkbox)
- ✅ الاسم الكامل
- ✅ الكود المرجعي
- ✅ رقم الجواز
- ✅ الجنسية
- ✅ الوظيفة
- ✅ العمر
- ✅ الحالة
- ⚠️ الإجراءات (بدون خط فاصل - آخر عمود)

### العروض الافتراضية:
```javascript
{
  checkbox: 48px,      // عمود التحديد
  name: 192px,         // الاسم الكامل
  referenceCode: 120px, // الكود المرجعي
  passport: 128px,     // رقم الجواز
  nationality: 128px,  // الجنسية
  position: 128px,     // الوظيفة
  age: 80px,           // العمر
  status: 120px,       // الحالة
  actions: 160px       // الإجراءات
}
```

### مثال على البيانات المحفوظة:
```javascript
// في localStorage تحت المفتاح: cvTableColumnWidths
{
  "checkbox": 60,
  "name": 250,
  "referenceCode": 100,
  "passport": 150,
  "nationality": 120,
  "position": 180,
  "age": 70,
  "status": 140,
  "actions": 200
}
```

---

## 💡 نصائح:

1. **للأعمدة الصغيرة** (العمر مثلاً): اجعلها أصغر لتوفير المساحة
2. **للأعمدة الكبيرة** (الاسم مثلاً): اجعلها أكبر لرؤية النص كاملاً
3. **الحد الأدنى**: لا يمكن جعل أي عمود أصغر من 50px
4. **RTL Support**: الميزة تعمل بشكل صحيح مع الاتجاه من اليمين لليسار
5. **💾 لا تقلق**: عدّل مرة واحدة والإعدادات ستبقى معك دائماً!

### 🎁 فوائد الحفظ التلقائي:
- ✨ **راحة المستخدم**: لا حاجة لإعادة التعديل في كل مرة
- 🎯 **تجربة شخصية**: كل مستخدم له تفضيلاته الخاصة
- ⚡ **سريع**: الإعدادات تُطبق فوراً عند فتح الصفحة
- 🔒 **موثوق**: يعمل حتى بدون اتصال بالإنترنت

---

## 🔧 للمطورين:

### إضافة الميزة لجداول أخرى:

1. **أضف State للعروض مع الحفظ التلقائي:**
```typescript
// القيم الافتراضية
const defaultColumnWidths = {
  column1: 100,
  column2: 150,
  // ...
}

// State مع قراءة من localStorage
const [columnWidths, setColumnWidths] = useState(() => {
  if (typeof window !== 'undefined') {
    const saved = localStorage.getItem('tableColumnWidths')
    if (saved) {
      try {
        return JSON.parse(saved)
      } catch (e) {
        console.error('Error parsing saved column widths:', e)
      }
    }
  }
  return defaultColumnWidths
})

// حفظ تلقائي عند التغيير
useEffect(() => {
  if (typeof window !== 'undefined') {
    localStorage.setItem('tableColumnWidths', JSON.stringify(columnWidths))
  }
}, [columnWidths])

// دالة إعادة الضبط
const resetColumnWidths = () => {
  setColumnWidths(defaultColumnWidths)
  toast.success('تم إعادة ضبط عرض الأعمدة')
}
```

2. **أضف دوال السحب:**
```typescript
const handleMouseDown = (e: React.MouseEvent, columnKey: string) => {
  // ...
}
```

3. **أضف الخطوط في رأس الجدول:**
```tsx
<th style={{ width: `${columnWidths.column1}px` }}>
  العنوان
  <div 
    className="absolute left-0 top-0 bottom-0 w-1.5 cursor-col-resize..."
    onMouseDown={(e) => handleMouseDown(e, 'column1')}
  />
</th>
```

4. **طبق العروض على الخلايا:**
```tsx
<td style={{ width: `${columnWidths.column1}px` }}>
  المحتوى
</td>
```

---

## 📱 الاستجابة:

- ✅ **الديسكتوب**: الميزة تعمل بشكل كامل
- ⚠️ **التابلت**: تعمل مع تجربة أقل قليلاً
- ❌ **الموبايل**: الجدول مخفي (يُستخدم عرض البطاقات)

---

## 🐛 استكشاف الأخطاء:

### المشكلة: الخطوط غير مرئية
- **الحل**: تأكد من أن الجدول يستخدم `position: relative` على `<th>`

### المشكلة: السحب لا يعمل
- **الحل**: تحقق من أن event listeners مضافة في useEffect

### المشكلة: العرض يقفز بشكل غير طبيعي
- **الحل**: تأكد من حساب `diff` بشكل صحيح (عكس الاتجاه لـ RTL)

---

## 🎉 الخلاصة

الآن يمكنك التحكم الكامل في عرض الأعمدة حسب احتياجاتك!

**ملاحظة**: التحذير عن `@theme` في `globals.css` هو تحذير عادي من Tailwind CSS v4 ولا يؤثر على الوظائف.

---

تم الإنشاء: نوفمبر 2024 ✨
