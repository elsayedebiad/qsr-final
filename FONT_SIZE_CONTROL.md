# 🔤 التحكم في حجم الخط - Font Size Control

## 🎉 ميزة جديدة: تكبير وتصغير خط الجدول

تم إضافة إمكانية التحكم الكامل في حجم خط الجدول مع حفظ تلقائي!

---

## ✨ الميزات:

1. **🔼 تكبير الخط** - زر A+ لتكبير الخط
2. **🔽 تصغير الخط** - زر A- لتصغير الخط
3. **📏 عرض الحجم الحالي** - يظهر الحجم بالبكسل
4. **🔄 إعادة الضبط** - إعادة الخط للحجم الافتراضي
5. **💾 حفظ تلقائي** - الإعدادات تُحفظ تلقائياً
6. **⚡ استجابة فورية** - التغيير يطبق مباشرة

---

## 🖱️ كيفية الاستخدام:

### في البانر فوق الجدول:

```
┌────────────────────────────────────────┐
│  [A-] [14px] [A+] [↻]  [📏 إعادة ضبط] │
└────────────────────────────────────────┘
```

### الأزرار:

- **A-** → تصغير الخط بمقدار 1px
- **A+** → تكبير الخط بمقدار 1px
- **14px** → الحجم الحالي (للعرض فقط)
- **↻** → إعادة الخط للحجم الافتراضي (14px)

---

## 📊 نطاق الأحجام:

```javascript
الحد الأدنى: 10px
الحد الافتراضي: 14px
الحد الأقصى: 20px
```

### أمثلة:
- **10px** → خط صغير جداً (للشاشات الكبيرة والبيانات الكثيرة)
- **12px** → خط صغير
- **14px** → حجم عادي (افتراضي) ⭐
- **16px** → خط كبير
- **18px** → خط كبير جداً
- **20px** → الحد الأقصى (لسهولة القراءة)

---

## 💾 الحفظ التلقائي:

### كيف يعمل:
1. عند تغيير حجم الخط → **يُحفظ فوراً** في localStorage
2. عند فتح الصفحة → **يُطبق تلقائياً**
3. لكل مستخدم → **إعداداته الخاصة**

### التخزين:
```javascript
// المفتاح
localStorage.getItem('cvTableFontSize')

// القيمة
"14" // رقم بالبكسل
```

---

## 🎯 حالات الاستخدام:

### 1️⃣ **شاشة صغيرة / بيانات كثيرة**
```
استخدم: 10-12px
الفائدة: عرض المزيد من البيانات في مساحة أقل
```

### 2️⃣ **استخدام عادي**
```
استخدم: 14px (الافتراضي)
الفائدة: توازن مثالي بين الوضوح والمساحة
```

### 3️⃣ **سهولة القراءة / ضعف نظر**
```
استخدم: 16-20px
الفائدة: خط واضح وسهل القراءة
```

### 4️⃣ **عرض تقديمي / مشاركة الشاشة**
```
استخدم: 18-20px
الفائدة: يمكن قراءته من بعيد
```

---

## 🎨 التفاصيل التقنية:

### الكود الرئيسي:

```typescript
// القيم الافتراضية
const defaultFontSize = 14
const minFontSize = 10
const maxFontSize = 20

// State مع قراءة من localStorage
const [tableFontSize, setTableFontSize] = useState(() => {
  const saved = localStorage.getItem('cvTableFontSize')
  if (saved) {
    const size = parseInt(saved)
    if (size >= minFontSize && size <= maxFontSize) {
      return size
    }
  }
  return defaultFontSize
})

// حفظ تلقائي
useEffect(() => {
  localStorage.setItem('cvTableFontSize', tableFontSize.toString())
}, [tableFontSize])

// دوال التحكم
const increaseFontSize = () => {
  setTableFontSize(prev => Math.min(prev + 1, maxFontSize))
}

const decreaseFontSize = () => {
  setTableFontSize(prev => Math.max(prev - 1, minFontSize))
}

const resetFontSize = () => {
  setTableFontSize(defaultFontSize)
}
```

### تطبيق على الجدول:

```tsx
<table 
  style={{ 
    fontSize: `${tableFontSize}px` 
  }}
>
  {/* ... */}
</table>
```

---

## 🚨 التنبيهات:

### عند الوصول للحدود:
- **الحد الأدنى** (10px):
  ```
  toast: "وصلت للحد الأدنى لحجم الخط 📏"
  الزر A- يصبح معطل
  ```

- **الحد الأقصى** (20px):
  ```
  toast: "وصلت للحد الأقصى لحجم الخط 📏"
  الزر A+ يصبح معطل
  ```

---

## 💡 نصائح:

1. **للعمل اليومي**: استخدم 14px (الافتراضي)
2. **لمراجعة بيانات كثيرة**: جرب 11-12px
3. **للعروض التقديمية**: استخدم 18-20px
4. **لسهولة القراءة**: 16px خيار ممتاز
5. **💾 لا تقلق**: الحجم يُحفظ تلقائياً!

---

## 🔄 التكامل مع ميزات أخرى:

### يعمل مع:
- ✅ تغيير عرض الأعمدة (Column Resizing)
- ✅ الفلاتر والبحث
- ✅ الترقيم (Pagination)
- ✅ Dark/Light Mode
- ✅ RTL Layout

### لا يؤثر على:
- ❌ عرض البطاقات (Mobile View)
- ❌ نماذج الإدخال (Forms)
- ❌ القوائم الجانبية (Sidebars)

---

## 📱 الاستجابة:

### Desktop (كبير):
```
الحجم الموصى به: 14-16px
جميع الأزرار مرئية
```

### Laptop (متوسط):
```
الحجم الموصى به: 13-15px
جميع الأزرار مرئية
```

### Tablet:
```
الحجم الموصى به: 14-16px
قد تكون بعض الأزرار مخفية
```

### Mobile:
```
الجدول مخفي (يُستخدم عرض البطاقات)
الميزة غير متاحة
```

---

## 🐛 استكشاف الأخطاء:

### المشكلة: الحجم لا يتغير
**الحل**: تحقق من أن الجدول يستخدم `style={{ fontSize }}`

### المشكلة: الحجم لا يُحفظ
**الحل**: تأكد من أن localStorage مفعّل في المتصفح

### المشكلة: الأزرار معطلة
**الحل**: هذا طبيعي عند الوصول للحدود (10px أو 20px)

### المشكلة: النص يتداخل
**الحل**: قلل حجم الخط أو وسّع الأعمدة

---

## 🎁 الفوائد:

### للمستخدم:
- 👀 **راحة للعين** - اختر الحجم المناسب لك
- ⚡ **سرعة العمل** - احفظ إعداداتك مرة واحدة
- 🎯 **مرونة** - غيّر حسب الحاجة
- 😊 **سهولة** - واجهة بسيطة وواضحة

### للنظام:
- 💾 **خفيف** - يُحفظ محلياً فقط
- 🚀 **سريع** - لا API calls
- 🔧 **بسيط** - سهل الصيانة
- ♿ **إتاحة** - يدعم accessibility

---

## 🔮 التحسينات المستقبلية:

خطط محتملة:
- 🎨 **Presets**: أحجام محددة مسبقاً (صغير/متوسط/كبير)
- ⌨️ **Keyboard Shortcuts**: Ctrl + Plus/Minus
- 📊 **Per-Column**: حجم مختلف لكل عمود
- ☁️ **Cloud Sync**: مزامنة بين الأجهزة
- 📱 **Mobile Support**: دعم العرض المحمول

---

## 🎊 الخلاصة:

**خط واضح = عمل مريح!**

الآن يمكنك ضبط حجم خط الجدول حسب راحتك واحتياجاتك، والإعدادات ستبقى معك في كل مرة! 🚀

---

**تم الإضافة**: نوفمبر 2024 🔤
**الإصدار**: 1.0
**الحالة**: ✅ مفعّل ويعمل
